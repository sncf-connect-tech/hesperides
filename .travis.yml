language: java
jdk: oraclejdk8
sudo: false

cache:
  directories:
    - "$HOME/.m2"
before_cache:
  - rm -rf $HOME/.m2/repository

stages:
  - build
  - test

jobs:
  include:
    - stage: build
      script: ./mvnw clean package -Dmaven.test.skip=true
    - stage: test
      script: ./mvnw test
