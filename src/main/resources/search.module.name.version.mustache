{
    "_source": ["name", "version", "working_copy"],
	"query": {
		"bool": {
			"must": [
				{
					"term": {
						"name": "{{name}}"
					}
				},
				{
					"query_string": {
						"default_field": "version",
                        "query": "{{version}}"
					}
				}
			],
			"must_not": [],
			"should": []
		}
	},
	"from": 0,
	"size": 100,
	"sort": [],
	"aggs": {}
}
