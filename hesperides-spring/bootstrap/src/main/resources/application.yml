spring.application.name: Hespérides Spring Boot

server:
  port: 8081
  contextPath: /rest

ldap:
  url:
  domain:
  userSearchBase:
  usernameAttribute:
  connectTimeout:
  readTimeout:

elasticsearch:
  host:
  port:
  index:
