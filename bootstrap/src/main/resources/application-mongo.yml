---
#spring.data.mongodb:
#  host: ${MONGO_HOST:localhost}
#  port: ${MONGO_PORT:27017}
#  database: ${MONGO_DATABASE:axonframework}
#  username: ${MONGO_USERNAME}
#  password: ${MONGO_PASSWORD}

hesperides:
  eventstore:
    host: ${EVENT_STORE_MONGO_HOST:localhost}
    port: ${EVENT_STORE_MONGO_PORT:27017}
    database: ${EVENT_STORE_MONGO_DATABASE:axonframework}
    username: ${EVENT_STORE_MONGO_USERNAME}
    password: ${EVENT_STORE_MONGO_PASSWORD}
  viewstore:
    host: ${PROJECTION_REPOSITORY_MONGO_HOST:localhost}
    port: ${PROJECTION_REPOSITORY_PORT:27017}
    database: ${PROJECTION_REPOSITORY_MONGO_DATABASE:test}
    username: ${PROJECTION_REPOSITORY_MONGO_USERNAME}
    password: ${PROJECTION_REPOSITORY_MONGO_PASSWORD}


